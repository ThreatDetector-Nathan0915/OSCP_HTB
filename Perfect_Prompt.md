Format the following walkthrough as a single, clean Markdown code block. I want it in one copy-pastable fenced Markdown blob like this:

markdown
Copy
Edit
# Title  
## Subsections  
\`\`\`bash  
commands  
\`\`\`  
Output and commentary  
Make sure the entire Markdown is inside one single triple-backtick code block (like above) so I can copy and paste it directly into a .md file without needing to stitch anything together. No extra text or explanation outside the code block. Just the Markdown blob.


Give me a full, verbose Hack The Box (HTB) machine walkthrough in a single raw Markdown block. The walkthrough should cover all steps from enumeration to full compromise, including command-line syntax, outputs, and explanations. Use a **clear, professional tone** suitable for study notes or a client-style report (no slang, no exaggerated hype, no emoji).

- Wrap the entire content in one continuous Markdown block (triple backticks with `markdown`)
- Include full bash and PowerShell commands
- After each important command line, add a **one- to three-line explanation** of the non-obvious flags (what they change on the wire or in the tool)
- Include tool outputs, observations, and concise analytical notes
- Do NOT break it into multiple code blocks or output sections outside that single outer block
- Do not include ANY text outside the Markdown block
- Use section headers for each phase (e.g. `## Step 1 — Initial recon`, `## Step 2 — Initial access`)
- Do not use emoji; keep formatting clean and readable
- Address the reader in neutral second person (“you”) where needed; avoid role-play or “mission” framing

I want to be able to copy and paste the entire response into a `.md` file without editing anything.
