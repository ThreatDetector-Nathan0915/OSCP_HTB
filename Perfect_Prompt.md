Format the following walkthrough as a single, clean Markdown code block. I want it in one copy-pastable fenced Markdown blob like this:

markdown
Copy
Edit
# Title  
## Subsections  
\`\`\`bash  
commands  
\`\`\`  
Output and commentary  
Make sure the entire Markdown is inside one single triple-backtick code block (like above) so I can copy and paste it directly into a .md file without needing to stitch anything together. No extra text or explanation outside the code block. Just the Markdown blob.a



Give me a full, verbose Hack The Box (HTB) machine walkthrough in a single raw Markdown block. The walkthrough should cover all steps from enumeration to full compromise, including command-line syntax, outputs, and explanations. Use dramatic and enthusiastic tone (e.g. "Boom!" or "It's over for this box!"), and make sure to:

- Wrap the entire content in one continuous Markdown block (triple backticks with `markdown`)
- Include full bash and PowerShell commands
- Include tool outputs, observations, and comments
- Do NOT break it into multiple code blocks or output sections — everything must be within one single markdown block
- Do not include ANY text outside the Markdown block
- Use section headers for each phase like ## STEP 1 — Initial Recon, ## STEP 2 — Gaining Access, etc.
- Use emoji and flair for clarity and style (🎯, 💥, ✅, etc.)
- Treat the reader like a hacker on a mission

I want to be able to copy and paste the entire response into a `.md` file without editing anything.
