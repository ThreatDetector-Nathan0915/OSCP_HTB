# 🧭 Internal Enumeration Summary – Multi-Host Network

---

## 🎯 Target: 192.168.127.111 – Windows Server w/ IIS + Tomcat

### 🔎 Open Ports:
```
21 (FTP)
80 (HTTP - IIS)
135 (RPC)
139 (NetBIOS)
443 (HTTPS - Tomcat 8.5.34)
445 (SMB)
5357 (WSDAPI)
8080 (Tomcat)
8443 (Tomcat Admin Panel)
```

### 🚨 Vulnerabilities Identified:
- **Apache Tomcat 8.5.34**
  - ✅ **Ghostcat (CVE-2020-1938)**  
    - AJP file read vulnerability  
    - Allows remote read access to sensitive files (e.g., `WEB-INF/web.xml`)  
    - CVSS: 9.8 – Critical

### 🛠️ Next Steps:
- Check if **port 8009 (AJP)** is open  
- Exploit Ghostcat to read config files or extract credentials  
- Use stolen creds to authenticate into Tomcat Manager or deploy a webshell

---

## 🐧 Target: 192.168.127.110 – Linux Box

### 🔎 Open Ports:
```
21 (FTP - vsftpd 3.0.5)
22 (SSH - OpenSSH 8.9p1)
```

### 🧪 Observations:
- vsftpd may allow **anonymous login**
- OpenSSH flagged for potential info leaks (low priority unless misconfigured)

### 🛠️ Next Steps:
- Try `ftp` anonymous access:
  ```bash
  ftp 192.168.127.110
  ```
- Check for file upload or download capability
- Attempt basic SSH brute force (if allowed)

---

## 🐧 Target: 192.168.127.112 – Linux w/ Apache

### 🔎 Open Ports:
```
21 (FTP)
22 (SSH)
80 (Apache HTTPD 2.4.52)
443 (HTTPS)
```

### 🚨 Vulnerabilities Identified:
- Apache 2.4.52 known to have:
  - Path traversal
  - CVE-2022-23943 (mod_sed DoS)
  - RCE potential if modules misconfigured

### 🛠️ Next Steps:
- Investigate `/cgi-bin/`, `.bak`, `.old`, `.txt` files via Gobuster
- Look for file uploads or known Apache vuln paths

---

## 🪟 Target: 192.168.127.206 – Windows Host

### 🔎 Open Ports:
```
135 (RPC)
139 (NetBIOS)
445 (SMB)
3389 (RDP)
5985 (WinRM/WSMAN)
```

### 🧪 Observations:
- No high-severity SMB vulns found
- **RDP + WinRM open** – lateral movement opportunity

### 🛠️ Next Steps:
- Try `smbclient` to list shares:
  ```bash
  smbclient -L \\192.168.127.206
  ```
- Use `crackmapexec` or `rpcclient` to enumerate users
- Test default or weak credentials via RDP or WinRM

---

## ✅ Priority Action Plan

| Host               | Action                                 | Goal                          |
|--------------------|----------------------------------------|-------------------------------|
| 192.168.127.111    | Exploit Ghostcat via AJP               | Read files, extract creds     |
| 192.168.127.110    | Test anonymous FTP / SSH brute         | Access via creds              |
| 192.168.127.112    | Scan Apache + check for vuln modules   | Exploit misconfig or upload   |
| 192.168.127.206    | Enumerate SMB + test WinRM/RDP         | Lateral move w/ valid creds   |

---

## 🔚 End Game
- Compromise Tomcat → Get creds → Access RDP or WinRM on internal Windows host
- Leverage Windows access for full network takeover

```bash
# Sample Ghostcat PoC Tool
python3 tomcat-cve-2020-1938.py -p 8009 -f WEB-INF/web.xml -t 192.168.127.111
```

Let me know if you need help decoding `web.xml`, crafting a WAR shell, or chaining privilege escalation!
